# Backend Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Base URL for generating links
BASE_URL=http://localhost:3001

# Document expiry (in days)
DOCUMENT_EXPIRY_DAYS=7

# Max upload size (in bytes) - 50MB
MAX_UPLOAD_SIZE=52428800

# Security (JWT for analytics access)
JWT_SECRET=your-secret-key-change-in-production

# ============================================
# Email Notifications (Resend)
# ============================================
# Get API key from https://resend.com
RESEND_API_KEY=re_123456789
EMAIL_FROM=alerts@yeetpdf.com

# ============================================
# Database (PostgreSQL)
# ============================================
# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Example: postgresql://postgres:password@localhost:5432/pdftolink
DATABASE_URL="postgresql://postgres:password@localhost:5432/pdftolink"

# ============================================
# AWS S3 Configuration (Optional)
# ============================================
# Leave these empty to use in-memory storage (demo mode)
# Set them to enable S3 storage with auto-expiration

# S3 Bucket name (create with: npm run setup:s3)
AWS_S3_BUCKET=

# AWS Region
AWS_REGION=us-east-1

# AWS Credentials (or use IAM roles / AWS_PROFILE)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Alternative: Use AWS CLI profile
# AWS_PROFILE=default
